# 五轮自我纠错日志

## Round 1: 数据量与主线定位

检查结论：

```text
SMIYC RoadObstacle 只有 30 张公开 GT。
RoadAnomaly21 只有 10 张公开 GT。
单靠 40 张 GT 不足以支撑“强结论”。
```

修改动作：

```text
把论文定位从“训练新检测器”改为“training-free road anomaly heatmap + risk-aware warning output”。
补入 StreetHazards partial，用作第三个公开数据源和困难域验证。
```

当前判断：

```text
小会议论文可行，但不能声称 SOTA。主张应该是轻量、可解释、能输出热力图/二值图/预警事件。
```

## Round 2: SMIYC 主实验检查

数据：

```text
SMIYC RoadObstacle / ObstacleTrack，30 张公开 GT。
```

结果：

| Method | AP | F1 | IoU | Precision | Recall | FPR95 |
|---|---:|---:|---:|---:|---:|---:|
| RoadContrast | 0.1169 | 0.1739 | 0.1108 | 0.1428 | 0.3690 | 0.4540 |
| RoadContrast + ERAS recall | 0.2180 | 0.2751 | 0.2068 | 0.3456 | 0.3981 | 0.4536 |
| DINO road prototype | 0.5203 | 0.5228 | 0.3998 | 0.4781 | 0.6864 | 0.0268 |
| DINO + ERAS light | **0.5271** | **0.5232** | 0.3989 | 0.4693 | 0.7524 | **0.0256** |

自纠：

```text
DINO 已经很强，ERAS 不是大幅提升器，而是风险校准和预警输出模块。
主结果应写成：ERAS light 在 AP/FPR95 上小幅提升，同时保留可解释 warning event。
```

## Round 3: RoadAnomaly21 泛化检查

数据：

```text
RoadAnomaly21，10 张公开 GT。
```

结果：

| Method | AP | F1 | IoU | Precision | Recall | FPR95 |
|---|---:|---:|---:|---:|---:|---:|
| RoadContrast | 0.3698 | 0.4473 | 0.3059 | 0.4022 | 0.6188 | 0.6409 |
| RoadContrast + ERAS balanced | **0.3733** | 0.4534 | 0.3099 | **0.4230** | 0.6137 | 0.6191 |
| DINO road prototype | 0.1324 | 0.3119 | 0.1907 | 0.1924 | **0.9314** | 0.6749 |
| DINO + ERAS balanced | 0.3227 | **0.4782** | **0.3289** | 0.3640 | 0.8369 | **0.5592** |

自纠：

```text
RoadAnomaly21 上 raw DINO 的 AP 很低，说明跨域不稳。
ERAS balanced 能明显改善 F1/IoU/FPR95，因此泛化论证应重点写“风险先验可稳定热力图”，而不是只看 SMIYC。
```

## Round 4: StreetHazards 标签与数据补充检查

数据：

```text
StreetHazards test 下载中断，但 partial tar 可解出 149 对 image/GT。
当前已标准化 paper_subset，并跑完 149 张 partial 子集。
```

自纠：

```text
第一次把 anomaly label 误设为 13，GT 明显不合理。
检查 DiCNet 代码和当前 raw label 后，修正为 anomaly label = 14。
```

结果：

| Method | AP | F1 | IoU | Precision | Recall | FPR95 |
|---|---:|---:|---:|---:|---:|---:|
| RoadContrast | 0.0295 | 0.0634 | 0.0343 | 0.0421 | 0.4158 | 0.6656 |
| DINO road prototype | **0.1133** | **0.1588** | **0.0975** | **0.1424** | 0.4781 | 0.3671 |
| DINO + ERAS light | 0.0691 | 0.1344 | 0.0753 | 0.1077 | 0.5177 | **0.2688** |
| DINO + ERAS balanced | 0.0626 | 0.1293 | 0.0722 | 0.1013 | 0.5432 | 0.2769 |

当前判断：

```text
StreetHazards 只能作为困难域补充。149 张结果显示 raw DINO 的 AP/F1 更高，而 ERAS light 的 FPR95 更低。
因此论文不能写成“ERAS 全面提升”，应写成“ERAS 在困难域更偏向降低误报，但会损失部分排序精度”。
```

## Round 5: 预警事件一致性检查

问题：

```text
旧版 warning_events.jsonl 来自 ERAS 中间组件，不一定和最终二值图完全一致。
这会影响论文里“二值图 -> bbox -> feedback to system”的逻辑闭环。
```

修改动作：

```text
已修改 src/raod_eras/experiment.py。
现在 warning event 从最终 heatmap 按最优阈值二值化后的连通域生成，并按 risk_score 排序。
字段包含 bbox、area、mean_score、threshold、road_overlap、lane_overlap、near_weight、system_action。
```

当前判断：

```text
论文可以明确写：系统输出不是只有热力图，还包含可被下游安全模块消费的结构化 warning event。
```

## Round 6: GitHub 发布可复现性检查

问题：

```text
旧版入口脚本把 ROOT 写死为 C:\Users\93785\Desktop\CIVS。
这在本机 PyCharm 能跑，但上传 GitHub 后别人无法复现。
requirements.txt 也漏了 scipy，虽然本机已安装，但新环境会缺少加速连通域依赖。
```

修改动作：

```text
scripts/run_research_experiment.py 改为 Path(__file__).resolve().parents[1] 自动定位项目根目录。
scripts/make_paper_assets.py 同样改为自动定位项目根目录。
src/raod_eras/config.py 默认 root 改为 Path.cwd()。
requirements.txt 增加 scipy。
新增 .gitignore、REPRODUCE.md、paper/release_checklist.md。
```

验证：

```text
已运行 py_compile。
已在桌面 CIVS 跑通：
python scripts\run_research_experiment.py --dataset road_anomaly --no-dino --max-samples 1 --out outputs\portable_root_smoke
```

当前判断：

```text
代码已经从“只适合本机临时实验”推进到“可以放 GitHub 复现”的状态。
还没完成的是官方 Springer 模板编译和最终 PDF。
```

## Round 7: CCIS 模板与发布包检查

问题：

```text
只有 Markdown/LaTeX 草稿还不等于可投稿。
必须确认 Springer/CCIS 模板能编译，参考文献能解析，图片能进入 PDF。
同时 GitHub/release 包不能混入 data、external、outputs 等大文件。
```

修改动作：

```text
从 Springer 官方页面下载 LaTeX2e Proceedings Template ZIP。
构建 paper/ccis_build，放入 llncs.cls、splncs04.bst、main.tex、references.bib、figures。
修复 LaTeX 中缺少 \cite{} 导致 bibliography 为空的问题。
补充 \titlerunning，解决标题 running head 警告。
新增 scripts/package_release.py。
打包脚本排除 data、external、outputs、模型权重、压缩包和缓存。
```

验证：

```text
pdflatex -> bibtex -> pdflatex -> pdflatex 已跑通。
生成 5 页 PDF：paper/RaOD-ERAS_CCIS_draft.pdf。
release zip 已生成：dist/raod_eras_release.zip。
检查 zip，未包含 data/、external/、outputs/、heatmaps/、binary_masks/、权重或压缩包。
```

当前判断：

```text
项目已经接近可投稿：有 CCIS 可编译 PDF、有实验、有图、有参考文献、有发布包。
仍需人工填写真实作者、单位、邮箱、基金/致谢，并按会议页数要求做最后排版。
```

## Round 8: 作者信息与自动编译脚本检查

问题：

```text
如果只告诉用户手动改 LaTeX，容易改坏 \author、\inst、\and 等命令。
第一次实现 set_paper_metadata.py 时确实因为正则匹配花括号导致 TeX 被破坏。
这说明投稿前元数据替换必须脚本化且可验证。
```

修改动作：

```text
在 paper_ccis_latex.tex 中加入 % METADATA_START / % METADATA_END 标记。
set_paper_metadata.py 改为替换标记区块，不再用正则匹配 \author{...}。
新增 build_ccis_pdf.py，自动复制模板文件、图片、bib，并执行 pdflatex/bibtex 编译链。
```

验证：

```text
set_paper_metadata.py 使用占位作者信息测试通过。
build_ccis_pdf.py 编译通过，生成 5 页 PDF。
当前只有轻微 overfull hbox 警告，无致命错误、无引用缺失。
```

当前判断：

```text
最后真正需要人工提供的只剩真实作者、单位、邮箱、基金/致谢。
给出这些信息后，可直接运行 set_paper_metadata.py + build_ccis_pdf.py 生成最终投稿 PDF。
```

## Round 9: PDF 视觉检查与投稿包检查

问题：

```text
第一次视觉检查发现两个排版问题：
1. Keywords 换行后单独留下一个点。
2. 4.2 Quantitative Results 标题后表格浮动到下一页，导致标题下面空转。
修复后又出现 6 页，其中第 6 页只有少量参考文献，页数不够紧凑。
```

修改动作：

```text
减少 keywords 数量，去掉孤立标点。
在 4.2 下增加一句结果概述，避免空标题。
缩小两张图为 0.86\linewidth。
表格使用 \small。
压缩 textfloatsep/floatsep/intextsep。
新增 scripts/package_submission.py，生成会议投稿专用 source zip。
```

验证：

```text
pdflatex/bibtex 编译通过。
使用 Poppler 将 PDF 渲染为 PNG，检查第 1、3、4、5 页。
最终 PDF 为 5 页。
表格、图片、参考文献可读，无重叠。
生成投稿包：dist/raod_eras_ccis_submission_package.zip。
投稿包只包含 PDF、main.tex、bib、figures、llncs.cls、splncs04.bst 和说明文件。
```

当前判断：

```text
项目已经达到“填真实作者信息即可投稿”的工程状态。
仍不能标记完全完成，因为真实作者/单位/邮箱/致谢尚未由用户提供。
```

## Round 10: 最终提交检查器

问题：

```text
项目已经能编译 PDF 和生成投稿包，但还缺一个机械检查，防止最终上传时忘记替换作者信息或误上传数据。
```

修改动作：

```text
新增 paper/author_metadata_template.json，集中记录作者元数据模板。
新增 paper/final_submission_checklist.md，列出最终投稿命令。
新增 scripts/final_submission_check.py，检查：
1. 作者/单位/邮箱 placeholder 是否仍存在。
2. PDF 是否存在且页数不超过 6。
3. CCIS submission zip 内容是否只包含 PDF、main.tex、bib、figures、llncs.cls、splncs04.bst、说明文件。
4. release zip 是否排除了 data、external、outputs、heatmaps、binary_masks、权重和压缩包。
```

验证：

```text
final_submission_check.py 已运行。
当前结果符合预期：只有 author_metadata_replaced 失败。
pdf_exists_and_page_count 通过：5 页。
submission_zip_contents 通过。
release_zip_excludes_large_artifacts 通过。
```

当前判断：

```text
可投稿状态的最后阻塞项已经被明确定位为真实作者信息。
一旦提供作者/单位/邮箱，运行 set_paper_metadata.py、build_ccis_pdf.py、package_submission.py、final_submission_check.py 即可进入最终上传状态。
```

## Round 11: 投稿系统表单字段检查

问题：

```text
会议系统通常不只上传 PDF，还需要手动填写 title、abstract、keywords、data/code availability、ethics、conflict、funding 等字段。
如果这些字段临时从 PDF 里复制，容易不一致或遗漏限制声明。
```

修改动作：

```text
新增 paper/submission_form_fields.md，整理 title、short title、abstract、keywords、main contribution、claim boundary、dataset statement、code availability。
新增 paper/statements.md，整理 data availability、code availability、ethics statement、conflict of interest、funding、acknowledgements、limitations。
更新 final_submission_checklist.md，把投稿系统字段填写加入最终流程。
```

验证：

```text
字段内容与当前 PDF 摘要、关键词、数据集声明和 claim boundary 保持一致。
这些文件会进入 release 包，方便投稿时复制。
```

当前判断：

```text
投稿材料不再只是一份 PDF；PDF、source zip、GitHub release zip、表单字段、声明文本都已准备。
唯一未完成项仍是真实作者元数据。
```

## Round 12: 作者元数据 JSON 流程检查

问题：

```text
只支持命令行参数替换作者信息仍然容易出错，尤其是作者较多、单位较长、需要致谢/基金时。
```

修改动作：

```text
增强 scripts/set_paper_metadata.py：
1. 默认读取 paper/author_metadata_template.json。
2. 仍支持命令行参数覆盖 JSON。
3. 支持 acknowledgements 字段。
4. 在 LaTeX 中加入 % ACKNOWLEDGEMENTS_START / % ACKNOWLEDGEMENTS_END 标记。
5. final_submission_checklist.md 改为推荐 JSON 流程。
```

验证：

```text
python scripts\set_paper_metadata.py --metadata paper\author_metadata_template.json 已通过。
python scripts\build_ccis_pdf.py 已通过，PDF 仍为 5 页。
python scripts\package_submission.py 已通过。
python scripts\package_release.py 已通过。
python scripts\final_submission_check.py 当前仍只因 placeholder 作者信息失败，其余检查通过。
```

当前判断：

```text
最终作者信息流程已经稳定。用户只需改 author_metadata_template.json，再运行四条命令即可得到最终可上传材料。
```

## Round 13: 一键最终构建流程检查

问题：

```text
即使已经有多个脚本，最终投稿前仍需按顺序运行 set_paper_metadata、build_ccis_pdf、package_submission、package_release、final_submission_check。
手动顺序执行仍有漏跑风险。
```

修改动作：

```text
新增 scripts/prepare_final_submission.py。
默认读取 paper/author_metadata_template.json。
如果作者信息仍是 placeholder，脚本会先停止，防止误生成“最终版”。
如需占位草稿，可加 --allow-placeholders。
```

验证：

```text
无 --allow-placeholders 时，脚本正确拦截 placeholder 作者信息。
带 --allow-placeholders 时，完整执行：
set_paper_metadata.py
build_ccis_pdf.py
package_submission.py
package_release.py
final_submission_check.py
最终仍因 placeholder 作者信息失败，其余检查通过。
```

当前判断：

```text
最终流程已经压缩为一条命令。
真实作者信息仍是唯一阻塞项。
```

## Round 14: 中文交接说明检查

问题：

```text
虽然最终流程已经脚本化，但如果让学生或合作者直接看英文 README 和 LaTeX 命令，仍然容易填错作者字段或上传错包。
```

修改动作：

```text
新增 paper/最后提交怎么做.md，用中文说明：
1. 只需要改 author_metadata_template.json。
2. 改完运行 prepare_final_submission.py。
3. PDF、投稿 source zip、GitHub release zip 分别是什么。
4. 投稿系统字段从哪些文件复制。

新增 paper/author_metadata_examples.md，给出单单位、多单位、导师参与、致谢/基金的 JSON 示例。
```

验证：

```text
文档已加入 README 和 final_submission_checklist.md。
不改代码和实验结果。
```

当前判断：

```text
项目已可交接给不熟悉 LaTeX 的同学执行最后一步。
仍需真实作者信息。
```
