# Paper Files

The current paper text is `riskprompt_paper_draft_en.md`. It corresponds to the corrected 189-pair evaluation protocol (`0=normal`, `1=anomaly`, `255=ignore`) and the compact results under `outputs/riskprompt_full_189/`.

The older RaOD-ERAS LaTeX/PDF files remain local historical artifacts and do not contain the final RiskPrompt-SAM results. Do not submit the old PDF. The next paper step is to transfer the current RiskPrompt draft and tables into the Springer CCIS template, add author metadata, and rebuild the PDF.

Current figure: `figures/riskprompt_qualitative.png`.
